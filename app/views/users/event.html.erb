<h1><strong><%= @user.nickname %></strong>さんのスケジュール</h1>
<div class='row'>
  <div class='col-xs-8'>
    <div id="calendar"></div>
    <%= link_to 'スケジュール追加', events_path(current_user.id) ,class: "btn-sm btn-success"%>
  </div>
  <div class='col-xs-4'>
    <span><strong><%= @user.nickname %></strong>さんの目標内容</span>
    <span><%= attachment_image_tag @user, :profile_image, fallback: "no_image.jpg", size: "60x60" %></span>
  </div>
  <div class='col-xs-4'>
  	<h4>通算勉強時間登録回数</h4>
  	<div><%= @user.stacks.count %>回！</div>
    <h4>累計日記数</h4>
    <div><%= @user.diaries.count %>回投稿！</div>
  </div>
  <div class='col-xs-4'>
  	<h4>目指す目標</h4>
    <% @user.goals.each do |user| %>
    <%=user.title %>
    <%= link_to "詳細", goal_path(user.id),class: "btn btn-primary" %><br>
    <% end %>
  </div>
</div>